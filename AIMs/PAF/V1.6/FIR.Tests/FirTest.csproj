<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>disable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>
  <ItemGroup>
    <!-- FIR brings ArcFace + FaceDatabase + FaceCrop; it already references
         VisualScene (for the SCRFD detector) and Core transitively. -->
    <ProjectReference Include="..\FIR\Mpai.Paf.Fir.csproj" />
  </ItemGroup>
</Project>
